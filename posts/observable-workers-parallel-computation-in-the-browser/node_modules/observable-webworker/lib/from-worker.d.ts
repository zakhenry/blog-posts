import { Observable } from 'rxjs';
export interface WorkerOptions {
    terminateOnComplete: boolean;
}
export declare function fromWorker<Input, Output>(workerFactory: () => Worker, input$: Observable<Input>, selectTransferables?: (input: Input) => Transferable[], options?: WorkerOptions): Observable<Output>;
