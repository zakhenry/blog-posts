import { Observable, ObservableInput } from 'rxjs';
export interface WorkerPoolOptions<I, O> {
    workerCount?: number;
    flattenOperator?: (input$: Observable<Observable<O>>) => Observable<O>;
    selectTransferables?: (input: I) => Transferable[];
}
export declare function fromWorkerPool<I, O>(workerConstructor: (index: number) => Worker, workUnitIterator: ObservableInput<I>, options?: WorkerPoolOptions<I, O>): Observable<O>;
